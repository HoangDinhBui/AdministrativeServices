@{
    ViewData["Title"] = "Đăng ký thường trú";
}

<div class="max-width-800 mx-auto">
    <div class="mb-4">
        <h2 class="fw-bold">Đăng ký thường trú (Mẫu CT01)</h2>
        <p class="text-muted">Vui lòng điền đầy đủ thông tin bên dưới để nộp hồ sơ.</p>
    </div>

    <form asp-action="CreateResidentRegistration" method="post" enctype="multipart/form-data">
        <input type="hidden" name="serviceTypeId" value="@ViewBag.ServiceId" />
        
        <div class="glass-card p-4 mb-4">
            <h5 class="fw-bold mb-3 border-bottom pb-2">1. Thông tin người đăng ký</h5>
            <div class="row g-3">
                <div class="col-md-12">
                    <label class="form-label">Họ và tên</label>
                    <input name="fullName" class="form-control" placeholder="NGUYỄN VĂN A" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Ngày sinh</label>
                    <input name="dob" type="date" class="form-control" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Giới tính</label>
                    <select name="gender" class="form-select">
                        <option value="Nam">Nam</option>
                        <option value="Nữ">Nữ</option>
                    </select>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Số CCCD/Định danh</label>
                    <input name="cccd" class="form-control" placeholder="012345678901" required />
                </div>
            </div>
        </div>

        <div class="glass-card p-4 mb-4">
            <h5 class="fw-bold mb-3 border-bottom pb-2">2. Địa chỉ đề nghị đăng ký</h5>
            <div class="mb-3">
                <label class="form-label">Địa chỉ chi tiết (Số nhà, đường, thôn/xóm, phường/xã, quận/huyện, tỉnh/thành)</label>
                <textarea name="address" class="form-control" rows="2" placeholder="Số 123, đường Láng, phường Láng Thượng, quận Đống Đa, Hà Nội" required></textarea>
            </div>
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Họ tên chủ hộ</label>
                    <input name="householdOwner" class="form-control" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Quan hệ với chủ hộ</label>
                    <select name="relationship" class="form-select">
                        <option value="Chủ hộ">Bản thân là chủ hộ</option>
                        <option value="Vợ/Chồng">Vợ/Chồng</option>
                        <option value="Con">Con</option>
                        <option value="Bố/Mẹ">Bố/Mẹ</option>
                        <option value="Khác">Khác</option>
                    </select>
                </div>
            </div>
        </div>

        <div class="glass-card p-4 mb-4">
            <h5 class="fw-bold mb-3 border-bottom pb-2">3. Hồ sơ đính kèm</h5>
            <p class="small text-muted mb-3">Tải lên các giấy tờ: Tờ khai CT01 (nếu có bản cứng), Giấy tờ nhà đất, Giấy khai sinh/Kết hôn...</p>
            <div class="mb-3">
                <input type="file" name="files" multiple class="form-control" />
            </div>
        </div>

        <div class="d-flex justify-content-between align-items-center mb-5">
            <a asp-action="Applications" class="btn btn-link text-muted">Hủy bỏ</a>
            <button type="submit" class="btn btn-primary px-5">Nộp hồ sơ</button>
        </div>
    </form>
</div>

<style>
    .max-width-800 { max-width: 800px; }
</style>
